enum ROUTE {
  HOME = "/",
  USER = "/users",
  TEAM = "/teams",
  PROMOTION = "/promotions",
  LOGIN = "/sign-in",
  AUTH = "/auth",
  FIELD = "/venue/:venueId",
  BOOKING = "/booking",
  VENUE = "/venue",
  PAGE_FORBIDDEN = "/403",
  PAGE_NOT_FOUND = "404",
}

export default ROUTE;
